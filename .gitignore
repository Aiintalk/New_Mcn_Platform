# 环境变量文件（禁止提交）
.env
.env.local
.env.*.local

# Python
__pycache__/
*.py[cod]
.venv/
.venv311/
venv/
.pytest_cache/
htmlcov/
.coverage
.coverage_baseline.json

# Node
node_modules/
dist/
build/
.cache/

# 日志
*.log
logs/

# 编译产物
*.pyc

# 报告文件（运行时生成，不提交）
backend/reports/

# 本地种子数据（含 API Key，禁止提交）
backend/seed_local.sql

# 运行时生成的文件（不提交）
backend/storage/

# Claude Code 本地配置（不提交）
.claude/

# 本地 skill 文件（不提交）
frontend/.od-skills/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# PM 私人记录（不进 git）
docs/pm/BUG修复登记.md

# 一次性 E2E seed 脚本（已跑完，保留工作区参考但不进 git）
backend/scripts/_e2e_seed_personas.py

.gstack/

# Legacy 数据迁移产物（含真实业务数据，一次性使用，禁止进 git）
backend/legacy_for_server_*.sql

# Walkthrough 生成产物（skill 自动产出，每次重新生成，不进 git）
docs/walkthrough/
