# M2 Sprint 24 红人工作台生产迁移链路：开发验收清单

## 开发侧准入

- [x] 根因与列表/详情差异有代码和数据库证据。
- [x] 先失败测试，再做最小通用修复。
- [x] 050 纳入数据库契约与部署链路。
- [x] 历史迁移 seed 不重放；校验和漂移和并发执行有防护。
- [x] 前端非 JSON 5xx 提示为低优先级小改动且有测试。
- [x] 生产备份、基线、执行、失败处理和复验步骤落档。
- [ ] PM 独立检查 diff 与测试证据。
- [ ] PM 批准生产数据库变更。
- [ ] PM 批准上线。

## 状态边界

- “代码已完成”：迁移执行器、部署入口、测试与文档已在功能分支形成提交并推送。
- “产品验收通过”：必须由 PM 独立复核后确认。
- “P0 线上已修复”：必须在 PM 批准后完成生产备份、迁移、部署和真实接口/页面复验；本开发任务不作该声明。

## 生产复验

1. `/api/health` 返回 200 且数据库健康。
2. `/api/tools/material-library/kols` 返回 200。
3. 红人 43 与另外 3 个红人详情均返回标准 JSON 信封和 200。
4. 红人 43 的旧素材可读；新建、编辑、文档元数据和视频字段可用。
5. 同一生产迁移命令第二次执行为 0 个新迁移，业务数据计数和内容不变。
