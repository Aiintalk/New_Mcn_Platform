# M2 红人工作台页面与交互一致性修正 v2 测试报告

> 执行日期：2026-07-15  
> 分支：`feature/kol-core-workflow`  
> 本报告只记录本轮页面与交互验收；不代表主分支已合并或已上线。

## 1. 定向自动化测试

| 范围 | 命令 | 结果 |
|---|---|---|
| 八个页签相关前端测试 | `npx vitest run src/__tests__/components/pages/KolWorkspacePage.test.tsx src/__tests__/components/pages/WorkspaceReferences.test.tsx src/__tests__/components/pages/WorkspaceRetrospective.test.tsx src/__tests__/components/pages/QianchuanWriterPage.test.tsx src/__tests__/components/pages/ValuesWriterPage.test.tsx src/__tests__/components/pages/LivestreamWriterPage.test.tsx src/__tests__/components/pages/FilmReviewModule.test.tsx --reporter=dot --silent` | 75 通过，0 失败 |
| 红人工作台相关后端测试 | `pytest -q tests/integration/routers/test_operator_workspace.py tests/integration/routers/test_operator_qianchuan_products.py tests/integration/routers/test_operator_qianchuan_writer.py tests/integration/routers/test_operator_values_writer.py tests/integration/routers/test_livestream_writer.py tests/integration/routers/test_operator_qianchuan_video_preview.py tests/integration/routers/test_operator_retrospective.py tests/unit/services/test_livestream_writer_file_parser.py tests/unit/services/test_qianchuan_writer_prompt.py tests/unit/tools/test_qianchuan_preview_prompt.py` | 169 通过，0 失败 |
| 价值观仿写结构化结果 | `pytest -q tests/integration/routers/test_operator_values_writer.py` | 27 通过，0 失败 |

对应 XML 证据：

- `/Users/zhangchong/Desktop/mcn_platform/New_Mcn_Platform-kol-core-workflow/artifacts/ui-v2-comparison/test-results/backend-workspace-focused.xml`
- `/Users/zhangchong/Desktop/mcn_platform/New_Mcn_Platform-kol-core-workflow/artifacts/ui-v2-comparison/test-results/backend-values-focused.xml`

## 2. 全量工程回归与归因

| 项目 | 当前分支结果 | 归因 |
|---|---:|---|
| 前端全量测试 | 306 通过，1 失败 | 失败项为 `SeedingWriterPage.test.tsx` 的 `chatStream` 断言；在干净 `origin/main@176e250` 同一命令定向复现为 22 通过、1 失败，因此是主分支既有门槛，不是本轮新增。 |
| 前端类型检查 | 20 个错误 | 与 v2 前提交 `1ec2c6c` 的 20 个错误完全一致；本轮新增为 0。主分支有 22 个同类历史错误。 |
| 前端构建 | 未完成构建产物 | `npm run build` 先执行类型检查，因此被同一组 20 个 v2 前类型错误阻断；不是新增构建错误。 |
| 后端最大可运行范围 | 1208 项中 1196 通过、8 个环境连接错误、3 个规范失败、1 跳过 | 8 个并发测试在 `origin/main@176e250` 同样因默认 `localhost:8000` 不可连接而失败。3 个规范失败在 v2 前提交 `1ec2c6c` 已存在：价值观三处操作日志约束、成片预审一个会话注册约束。 |
| 主分支规范守卫 | 6 通过 | `origin/main@176e250` 的 `tests/integration/test_convention_guard.py` 通过，说明当前 3 个规范失败不是主分支基线，而是 v2 前功能分支存量。 |

归因复现命令均使用同一 Python 虚拟环境和验收环境变量；未把任何验收数据写入主分支工作树。

## 3. 真实浏览器主流程

固定尺寸为 1440×900、1024×768；新版使用“测试达人”和“【虚拟验收数据】夏日眼周护理组合”。所有资料均明确为虚拟验收数据，不构成真实商品、投放或效果承诺。

| 页签 | 已验证主流程 | 结论 |
|---|---|---|
| 人物档案 | 五分区展示、编辑、取消、保存、刷新与红人隔离 | 通过 |
| 产品库 | 完整商品卡、新建、编辑、软删除、选择唯一当前商品 | 通过 |
| 千川仿写 | 初稿、运营意见、第一轮预审、最终确认、保存、`.txt` 导出 | 通过 |
| 价值观仿写 | 情绪方向、真实模型结构化初稿、一轮修改、相似度、历史、文本导出 | 通过 |
| 直播仿写 | 三种卖点顺序（含“背书→种草→机制”）、锁定对标、生成、修改、导出 | 通过 |
| 千川成片预审 | 两条完整视频上传、真实完整视频分析、报告保存、办公文档导出 | 通过 |
| 复盘 | 五份不同 Markdown、`.xls`、逐份解析、分析详情、办公文档导出 | 通过 |
| 素材库 | 文档解析、视频上传播放、文本编辑保留视频、替换视频、二次确认删除 | 通过 |

素材库删除的网络闭环：确认按钮触发 `DELETE /api/tools/material-library/kols/2/references/5`，返回 200 与“删除成功”；重新加载后条目消失。此前失败由自动化脚本错误点击了底层“删除”按钮，确认弹层实际按钮为 `.ant-popover .ant-btn-primary`，不是产品代码缺陷。

## 4. 已知工程门槛

1. 前端全量测试中的种草仿写单测失败为主分支可复现的既有问题，不纳入本轮功能缺陷。
2. 前端 20 个类型错误、后端 3 个规范失败均早于 v2 页面修正提交；本轮未修改无关字幕、导出、旧测试或管理端配置。
3. 上述门槛应在合并前由产品负责人决定修复或批准例外；本报告不把它们表述为已解决。
