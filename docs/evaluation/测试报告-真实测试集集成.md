# 张翀真实测试集集成 — 测试报告（#79）

> 分支：`feature/eval-real-testcases`（基于 main cb3f4ca）
> 方案：A（纯业务数据 + 剥离 messages 死字段 + 停用 demo）
> 日期：2026-08-18
> 数据源：`~/repo_aikol/project/meetings/0806_weekly_meeting/测试脚本_更新字段.et`（0818 PM 补齐字段版）

---

## 1. 变更清单

| 文件 | 类型 | 说明 |
|---|---|---|
| `backend/scripts/seed_eval_testcases_real.py` | 新增 | 10 条真实测试例（自包含嵌入 + 幂等 upsert by name） |
| `backend/migrations/057_eval_testcases_real_data.sql` | 新增 | ① 剥离 `input_payload.messages` 死字段 ② 停用 demo tag 测试例 |
| `backend/tests/unit/evaluation/test_seed_eval_testcases_real.py` | 新增 | 8 个数据契约测试 |
| `docs/evaluation/张翀真实测试集-集成方案说明.md` | 新增 | 方案文档（A/B/C 对比，PM 已拍板 A） |
| 本报告 | 新增 | — |

**0 业务代码改动**（不动 `app/`）。

## 2. 数据契约（8 测试全绿）

- 10 条、`name` 唯一（前 3 条保留张翀钩子分类 + 产品后缀 + ①②序号；其余「达人 · 产品」式）
- 4 输入字段（达人/人设/产品信息/参考脚本）全非空
- **无 messages**（方案 A 核心：指令归版本提示词）
- tags 含 `真实数据` 溯源 tag，与 `demo` 互斥
- 达人分布：羊羊×3 / 暖暖×3 / 姜周周×2 / 可可×2
- 源文件一致性锚点（R1/R5/R10 内容前缀逐字对齐）
- 迁移 057 含剥离/停用语句

## 3. dev 库（mcn_m1）执行结果

| 步骤 | 结果 |
|---|---|
| 迁移前 | 13 条全含 messages，13 条 demo 全激活 |
| 执行 057 | UPDATE 13（剥 messages）+ UPDATE 13（停 demo） |
| 执行后 | messages=0，demo 激活=0 |
| seed 真实数据 | 插入 10 |
| 幂等重跑 | 插入 0 / 更新 10 ✅ |
| 终态 | 23 条（真实 10 全激活 / demo 13 全停），全库 messages=0 |

## 4. 架构对齐（方案 A 落地）

```
input_payload = { name(达人名), persona(人设), product_info, original_script }
                 ↑ 只放业务素材，无指令
指令 = version.config_payload.system_prompt_template（版本提示词，generator 渲染时自构 messages）
```

demo seed（`seed_eval_testcases_demo.py` / `seed_eval_demo.sql`）的 messages 历史包袱由迁移 057 全库清理，DB 与真实数据结构就此一致。

## 5. 后续

- 下批真实数据：仿 `seed_eval_testcases_real.py` 增批次
- 版本提示词模板完善：独立事项（产品侧），测试例侧已就绪
