# 评测模块后续迭代路线图

> 依据：PM 0828 裁决（基于 v2 设计稿预留 + 会话积累的规划点整理）
> 状态：✅ PM 已对齐优先级；作为后续迭代的排期依据
> 关联：`docs/superpowers/specs/2026-08-27-eval-scoring-strategy-design.md`（策略层设计存档）·
> `docs/superpowers/specs/2026-07-20-aigc-evaluation-system-design.md`（v2 设计稿预留章节）

---

## 近期（高价值，建议逐个成 PR）

| # | 方向 | 说明 | 依赖/现状 |
|---|---|---|---|
| 1 | **张翀第二批测试集录入** | 第一批 10 条已就位（`真实数据` tag）；后续批次按 `seed_eval_testcases_real.py` 范式增补，机制现成 | 等张翀数据 |
| 2 | **worker 部署加固** | trigger 端 redis 故障优雅降级（现会 500）+ web/worker 分进程部署（生产 PM2 分离） | 无依赖，纯加固 |
| 3 | **评委模型管理增强** | `eval_judge_models` 候选池表已建未消费。接入管理 UI + 版本/运行级选用；可扩展多评委互验（glm+kimi 交叉评分）/评委一致性校验，提升评分可信度 | 表已有，做消费侧 |

## 中期（条件成熟时启动）

| # | 方向 | 说明 | 启动条件 |
|---|---|---|---|
| 4 | **安雅品类 rubric 变体** | scenario_tag 机制已留（UI 已按 PM 裁决隐藏）；安雅写美妆/减肥变体内容后在维度页补 | 安雅提供变体内容 + 随策略层一起放开 UI |
| 5 | **定时策略消费** | `eval_schedule_policies` 表有 CRUD 无调度执行；cron 定时触发 run，版本迭代后自动回归 | 手动跑高频到嫌烦时 |
| 6 | **评测策略层实施** | 设计已存档（0827 文档 + 六项裁决已定）：业务套餐、rubric 变体选择、轻量规则快照、对比页规则差异标注 | 分业务评测需求真实出现（4-8 种套餐） |

## 远期（v2 设计稿预留，记录在案）

| # | 方向 | 说明 |
|---|---|---|
| 7 | 生产数据回捞 | outputs/用户反馈 → AI 筛选（质量/多样性/异常值）→ 候选 case 池 → 审核入库，测试集自动补充 |
| 8 | 用户 case 反馈入口 | AIGC 工具页"结果不好用"→ 提交最终采用版本（0724 周会定的生成-使用-反馈闭环，归前端工具侧） |
| 9 | 测试集批量导入导出 | CSV/Excel 批量维护（现单条 CRUD） |
| 10 | 评测复制到 N 工具 | 现锚定千川仿写；直播间脚本仿写/价值观仿写接入（`tool_code` 字段已留） |

---

## PM 裁决记录（0828）

- 安雅变体（原近期#2）→ **中期**
- 定时策略（原近期#3）→ **中期**
- 评委模型管理（原中期）→ **近期高优**
- 远期四项维持，记录在案
- 策略层启动条件维持"业务需求出现才做，不预先建设"
