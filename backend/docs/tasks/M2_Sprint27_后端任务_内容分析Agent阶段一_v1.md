# M2 Sprint27 后端任务：内容分析 Agent 阶段一 v1

> 状态：开发侧离线验收已完成，等待主产品经理独立审核。未合并、未部署、未上线。
> 正式产品依据：外部 PM（产品经理）文档《M2 对标内容分析智能体需求文档》v1.6；外部文件只读，不复制、不修改、不纳入仓库。
> 最终代码对象：`3214024b3ad7dec0b3a2c8813f902be5db153883`（阶段一代码收口提交）。

## 一、任务目标

在不新增接口、数据表、迁移、调度、前端和真实模型调用的前提下，交付数据源与分析器均可替换的离线内核，用自动化测试锁定阶段一可以确定的计算、证据与项目隔离边界。

## 二、开发范围

1. 建立不可变领域对象，表达内容来源、稳定身份、最新互动值、证据、项目上下文、业务状态、日报和内容库候选。
2. 实现中国时区下的最近 3 个与 30 个完整自然日窗口、稳定键去重、人设点赞五项基准和播放量不可用处理；每个账号独立生成最近 3 日千川点赞前 3 池，接入主引擎后再按项目筛选 0—3 条机会。
3. 定义 `ContentAnalyzer`（可注入的异步内容分析协议），测试只使用模拟分析器；共享基础分析可复用，项目适配判断必须分项目执行。
4. 实现 `ContentAnalysisEngine`（离线内容分析编排器），输出项目日报、分类和互动概览、0—3 条人设/千川机会、待入库候选与方法级跨项目候选；候选门禁要求六类封闭价值信号（早期数据强、相对基准更优、新选题或结构、清晰流量钩子、可复用转化结构、值得关注的镜头表现）、覆盖项目人设/目标用户/内容规划/运营方向的结构化适配理由、视频引用与转写双证据，并与项目内已有库的全部稳定键比较。
5. 强制证据保护：缺少字幕可继续；视频不可读时不保留确定镜头或第一画面结论；来源商品说法不得升格为当前项目已确认事实。

## 三、数据与测试输入口径

- 目标飞书 Base（多维表格）只读地址已解析，但本会话令牌缺少 `base:table:read`（读取多维表格数据的权限）。因此本轮没有读取或提交原始 2098 条；这是访问权限不足，不是数据不存在。
- `phase1_anonymized.json`（阶段一匿名静态样本）仅含 3 条静态匿名合成内容样本，只用于字段与隐私形态检查。它不含项目关系，也不经适配层驱动引擎。
- 多项目关系与项目隔离另由 `test_engine.py`（离线引擎单元测试）中的独立匿名合成对象验证，不得将这一证据归给上述 3 条静态样本。

## 四、交付物

| 类型 | 路径 | 说明 |
|---|---|---|
| 离线内核 | `backend/app/services/content_analysis/` | 领域模型、确定性计算、分析协议和项目隔离编排 |
| 静态样本 | `backend/tests/fixtures/content_analysis/` | 3 条静态匿名合成内容样本，只用于字段/隐私形态检查 |
| 自动化测试 | `backend/tests/unit/services/content_analysis/` | 63 条离线测试，覆盖计算、证据、状态、候选门禁与隔离边界 |
| 实施计划 | `docs/superpowers/plans/2026-09-03-content-analysis-agent-phase1.md` | 已确认范围的任务拆分与验证命令 |
| 验收与测试 | 本任务配套验收文档、测试报告 | 记录 CA-01 至 CA-23 覆盖状态和完整验证证据 |

## 五、明确不做

- 不做公共 API（应用编程接口）、正式数据接口、生产飞书读取、数据库/迁移、路由、调度、前端、真实项目验收。
- 不做可读视频的完整镜头验收，不调用真实或付费模型。
- 不生成公司级日报，不构造互动时间序列或增长趋势。
- 不实际持久化日报、基准、内容库或跨项目候选，不联调下游仿写读取。
- 不建设人工入库内容补标/回写、库生命周期、停用/恢复或编辑/版本能力。

## 六、验收准入

1. 63 条内容分析聚焦测试全部通过，聚焦覆盖率为 97%。
2. `unit + integration`（单元测试与集成测试）最大回归在正确后端目录下无失败。
3. 仓库覆盖率分层门禁给出可重复的完整证据，无论结果均如实记录。
4. `compileall`（Python 语法编译检查）和 `git diff --check`（Git 差异格式检查）通过。
5. README（项目说明）、PM 状态、任务、测试和验收文档全部落档，且不虚构生产能力。

## 七、发布边界

本任务的“通过”只指开发侧离线内核验收通过。后续仍须由主产品经理独立审核，并在阶段二完成正式数据源、持久化、调度、下游联调和真实模型/视频质量验证。
