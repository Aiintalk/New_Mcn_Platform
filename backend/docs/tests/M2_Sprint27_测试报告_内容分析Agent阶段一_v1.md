# M2 Sprint27 后端测试报告：内容分析 Agent 阶段一 v1

> 日期：2026-09-03
> 分支：`codex/content-analysis-agent-dev`（内容分析开发分支）
> 验收验证对象：基线 `9adfc83926c44091a5b57a1ac854bd202f85d31b` 加当时尚未封存的阶段一返修差异；该差异后续已通过主产品经理第二轮审核和用户产品验收，并获授权封存到当前独立开发分支

## 一、结论

返修后内容分析专项测试 313/313 通过，专项覆盖率 92.2%。最新完整覆盖率门禁在 `backend`（后端目录）执行，收集 2327 条，2326 通过、0 失败、0 错误、1 跳过、14 条警告，退出码为 0。开发侧离线切片可交主产品经理第二轮独立审核。

此结论不包含公共 API（应用编程接口）、正式数据源、数据表/迁移、调度、路由、前端、下游联调、真实项目效果、可读视频完整镜头或真实/付费模型质量。

## 二、环境与隔离

- Python（编程语言）：3.10.9；pytest（Python 测试工具）：9.1.1。
- 数据库配置只使用本地测试地址，`DATABASE_URL`（数据库连接地址）和 `JWT_SECRET`（测试签名密钥）均为本轮测试值。
- 未执行生产数据库操作、迁移、外部调用、真实或付费模型调用。
- 2 条静态 fixture（测试固定输入）是最小匿名合成内容，另含 1 条匿名项目关系和 1 份匿名上下文；样本经标准适配器转为领域对象并驱动一次离线引擎。多项目关系由 `test_engine.py`（离线引擎单元测试）中的独立匿名对象验证。

## 三、执行记录

### 1. 最终内容分析聚焦测试

```bash
cd backend
env DATABASE_URL='postgresql+asyncpg://postgres:postgres@localhost:5432/mcn_platform_test' JWT_SECRET='content-analysis-local-test-secret-at-least-32-characters' .venv/bin/pytest -q tests/unit/services/content_analysis --cov=app/services/content_analysis --cov-report=term-missing
```

> 中文说明：在后端目录运行内容分析专项单元测试并单独统计该模块覆盖率，只使用本地测试配置。

- 最终结果：313 通过，0 失败，耗时 2.50 秒，退出码 0。
- 专项总覆盖率：92.2%；公开入口 100%、适配层 88%、分析边界 94%、确定性逻辑 95%、领域模型 91%、引擎 92%。

### 2. 前次长时间门禁定位（非最终验证）

- 主产品经理发现前次门禁进程运行约 1 小时 26 分钟且持续高 CPU 后，本开发会话立即停止对应进程，没有继续把静默当作正常。
- 最后进度指向 `test_qianchuan_collection.py::TestParseFile::test_parse_md_success`（千川文件解析测试）；该单测为 1/1 通过，整文件为 31/31 通过。
- 与相邻测试组合为 46/46 通过；路由前半分片为 418/418 通过，后半分片为 775/775 通过，完整路由套件为 1312/1312 通过；未复现该单测或固定分片死循环。
- 追加视觉边界反例后启动的第一次完整门禁因代码仍需返修而主动停止；测试会话没有走到删表清理。修复后的下一轮因本机 `mcn_test`（测试数据库）遗留行出现 16 条重复计数失败，覆盖率脚本仍返回 0。核验测试库名称后只删除该本地测试库的 66 张元数据表，剩余公开表为 0；16 条原失败用例随后 16/16 通过。
- 封存前最终门禁增加 `/opt/homebrew/bin/timeout 1200`（20 分钟硬停止）和 `faulthandler_timeout=300`（单测试运行 5 分钟时输出调用栈），并从干净测试库启动，在 7 分 10 秒正常完成。
- 仓库既有 `run_coverage.py`（覆盖率脚本）没有传播 pytest（Python 测试工具）失败退出码；本轮未越界修改公共测试基础设施，最终门禁同时以测试摘要 0 失败、0 错误和覆盖率六层达标为通过条件。

### 3. 最新完整覆盖率门禁（最终证据）

```bash
cd backend
/opt/homebrew/bin/timeout 1200 env DATABASE_URL='postgresql+asyncpg://postgres:postgres@localhost:5432/mcn_platform_test' JWT_SECRET='content-analysis-local-test-secret-at-least-32-characters' PYTEST_ADDOPTS='-o faulthandler_timeout=300' .venv/bin/python scripts/run_coverage.py --gate
```

> 中文说明：在正确后端目录运行仓库覆盖率脚本，同时执行单元与集成测试，检查整体和六个代码分层的最低覆盖率。

| 收集 | 通过 | 失败 | 错误 | 跳过 | 警告 | pytest 计时 | 退出码 |
|---:|---:|---:|---:|---:|---:|---:|---:|
| 2327 | 2326 | 0 | 0 | 1 | 14 | 430.08 秒 | 0 |

### 4. 测试先行返修证据

- 第一组新增阻断用例修复前为 8 失败、114 未选中；覆盖结构化同步问题、无依据效果结论、来源方法误杀/漏放、跨项目来源证据和最终结果不可变性。
- 第二组补充用例修复前为 4 失败、6 通过、116 未选中；覆盖项目判断自由文本、媒体错误脱敏、坏项目不阻断好项目和缺选题不得入库。
- 最后一组独立复审反例修复前为 5 失败、4 通过、122 未选中；覆盖近义绩效/视觉话术、保存方法中的品牌与人物、同步缺口跨账号污染，以及基础分析与项目判断永久挂起。
- 独立复审构造四类边界字段夹带“已确认首帧展示商品”的反例，修复前 4/4 失败；统一收紧方法、来源、开头和正文边界后，四条反例与两条相邻保护用例 6/6 通过。
- 修复后第一轮聚焦回归 20/20 通过；补充近义绩效话术、保存方法来源身份、跨账号同步缺口和分析器超时的 9 项聚焦回归通过，最终内容分析专项 313/313 通过。

### 5. 最终新增关键场景

- 每账号独立取最近 3 日千川点赞前 3，接入主引擎后由项目再筛 0—3；第 4 名不回填、不入库。
- 六类封闭价值信号：`early_data_strength`（早期数据强）、`relative_benchmark_outperformance`（相对基准表现更优）、`novel_topic_or_structure`（新选题或新结构）、`clear_traffic_hook`（清晰流量钩子）、`reusable_conversion_structure`（可复用转化结构）、`notable_shot_performance`（值得关注的镜头表现）。
- 结构化适配理由覆盖 `project_persona`（项目人设）、`target_users`（目标用户）、`content_plan`（内容规划）和 `operating_direction`（运营方向）。
- 待入库候选要求视频引用+转写双证据追溯。
- 平台内容编号与外部链接的交叉去重，以及跨账号稳定身份冲突拦截。
- `FAILED`（失败）和近期 `SUCCESS_WITHOUT_CONTENT`（成功且无内容）旧载荷过滤，`PARTIAL_SUCCESS`（部分成功）显式限制。
- 同步缺口使用受影响账号、覆盖窗口和影响类型的结构化合同；单项目非法合同不阻断其他有效项目。
- 同步缺口只能指向本次同步账号，不能携带其他项目账号；单项目非法合同不阻断其他有效项目。
- 分析器自由文本不能伪造播放互动率、逐日增量、近义趋势、视觉、投放或成交效果；不可读媒体的原始错误不会进入报告。
- 基础内容分析与项目适配均有可配置超时；永久挂起或单条异常只形成受影响项目的数据限制，不阻断其他内容或项目。
- 泛化“使用方法”不会被任意三字重合误删，价格/功效的语义改写和已保存来源证据不能进入跨项目方法池。
- 待入库候选自包含结构化项目适配理由和六类价值依据。
- 待入库候选必须同时包含非空选题和摘要。
- 有转写、无视频时，语言开头中混入的视觉证据会被移除，合法转写证据保留，并生成视觉限制说明。
- 无可读画面时，方法、来源、开头或正文适用边界均不得夹带确定视觉事实；“仅复用画面结构”这类中性方法边界可保留。

## 四、覆盖率明细

| 分层 | 实际 | 目标 | 结果 |
|---|---:|---:|---|
| `app/core/`（核心基础设施） | 100.0% | 90% | 通过 |
| `app/models/`（数据模型） | 100.0% | 90% | 通过 |
| `app/services/`（业务服务） | 89.1% | 80% | 通过 |
| `app/routers/`（接口路由） | 73.0% | 70% | 通过 |
| `app/adapters/`（外部服务适配器） | 80.3% | 60% | 通过 |
| `app/middlewares/`（请求中间件） | 100.0% | 90% | 通过 |
| 整体 | 80.4% | 48% | 通过 |

最新门禁脚本报告整体覆盖率 80.4%，六个分层全部通过。

最新内容分析专项覆盖率为 92.2%，逐文件数值来自同一次 313 项专项运行。

## 五、警告与跳过项

- 14 条警告均来自 `tests/unit/services/test_tikhub_adapter.py`（TikHub 适配器单元测试），对应 `RuntimeWarning`（运行时警告）：模拟的异步调用没有被等待。它们在本轮内容分析目录之外，未做无关修复。
- 1 条跳过为 `test_oss_upload_download_delete_round_trip`（对象存储真实上传、下载、删除往返测试），需真实 OSS（对象存储）凭证，不在本轮允许的外部调用范围。

## 六、静态检查与测试副作用

```bash
.venv/bin/python -m compileall -q app/services/content_analysis tests/unit/services/content_analysis
git diff --check
```

> 中文说明：第一条编译内容分析源码和测试，用来发现 Python 语法错误；第二条检查 Git 差异里的空白和冲突格式问题。两者退出码均为 0。

本轮最终门禁结束后未发现覆盖率报告、Word 文档或其他新增测试副作用文件；工作树新增项仍只有本轮标准适配器及其专项测试。

## 七、未验证项

- 未在生产逻辑中读取飞书 Base（多维表格）、日期表或本地关系快照，也未提交原始 2098 条。
- 未验证正式数据接口、持久化、定时扫描、下游仿写读取、库生命周期或人工入库补标。
- 未验证真实项目的分类与适配质量，也未验证可读视频的完整镜头分析质量。
- 验收测试执行时未暂存、未产生新提交、未推送、未建合并请求、未合并、未部署、未上线；后续用户仅授权本地提交和推送当前独立开发分支，仍未授权合并请求、合并、部署或上线。
