# M2 Sprint25 后端测试报告：达人档案统一 v1

> 日期：2026-08-04（PM 独立验收返修后更新）
> 分支：`feature/kol-persona-profile-unification`
> 起始提交：`5264628d3a0270e010ed1178e56fd6d0ebce67bd`
> 结论边界：开发侧技术门禁通过，等待产品经理验收；未执行生产数据库迁移。

## 一、验证范围

- 正式 `kols` 数据源、分页搜索和 `kol_id` 参数校验。
- 当前运营按 `kol_id` 查询关联入驻资料，跨运营、未绑定历史和姓名相同数据不得混入。
- 生成请求、人格报告、产出、日志、历史详情与档案同步的 `kol_id` 一致性。
- 定位字段空值自动写、非空逐字段确认、默认保留及提交时加锁重算。
- 五项事实字段只接受报告原文证据、只填空、不覆盖人工内容。
- 红人管理和旧素材库写入口收口；工作台单字段更新；文案、直播、复盘等下游上下文兼容。
- 迁移全链重放、历史未绑定数据保留、重复执行、外键和索引。

## 二、自动化结果

| 门禁 | 结果 | 说明 |
|------|------|------|
| 返修定向测试 | 修复前 7 失败、16 通过；修复后 23/23 通过 | 覆盖四种报告结构异常、成功重试有/无写入、重试再次失败 |
| 后端相关与邻接回归 | 848/848 通过 | 最终代码串行执行，117.47 秒；原 841 项加本次 7 个返修场景，覆盖全部后端单元测试、约定守卫、正式达人、入驻绑定、档案同步和下游消费者 |
| 迁移自动化专项 | 1/1 通过 | 从 `backend/` 目录执行，验证 055 重复执行和历史空编号保留 |
| 并发同步专项 | 通过 | 真实 PostgreSQL 行锁下重新计算覆盖决定 |
| 迁移专项 | 通过 | 从空库顺序执行至 055，再重复执行 055 |
| 约定守卫 | 通过 | 标准响应、操作日志和会话补丁规则均在相关套件内 |

相关完整回归覆盖模型、迁移、入驻绑定、人格同步、正式上下文、提示词、人格定位、红人管理、旧素材库、工作台、下游兼容和约定守卫。执行时使用本地测试库，未连接生产数据库。

848 项回归伴随 14 条项目既有的 TikHub 适配器模拟协程未等待警告；测试仍全部通过，本功能没有改动该适配器。直接从仓库顶层收集全部 `backend/tests/` 时还会触发既有 `tests/intake/conftest.py` 的新版 pytest 插件位置限制，因此本报告以显式相关与邻接清单的完成结果为技术门禁，不把该收集错误写成业务通过。

独立只读技术复审首轮发现的生成中达人删除、部分流归档、同步失败误报和历史覆盖确认恢复问题，均先补失败测试再修复。新增用例确认：只有完整结束的模型流才能归档；归档前再次锁定并校验正式达人；失败报告不生成产出、不写档案；详情接口真实返回生成、定位同步和事实补全失败状态。

PM 独立验收返修继续采用测试先行：修复前新增用例得到 `7 failed, 16 passed`。最小修复后，缺分隔符、任一侧为空和异常多段均进入 `failed`，不生成 Output、不写七字段；工作台事实补全按同一 `report_id` 的最新同步日志判定，成功但零写入也记录完成，后续再次失败则恢复失败状态。提交前追加的旧格式成功日志兼容断言先红 1 项，再修复为同时识别旧日志明细和新报告级日志。最终定向测试为 23/23，通过后再完成 848 项相关与邻接回归。

本轮独立只读技术复审最终结论为 `APPROVED`：异常双段归档、最新事实状态、旧日志兼容、日志脱敏和 10 个文件提交白名单均无阻断；复审角色未修改文件。

## 三、迁移验证

临时数据库从 `001` 顺序重放到 `055`。由于既有迁移 `031/034/035` 依赖已存在的模型编号，空库重放时在 `003` 后加入迁移前置的测试模型数据，再继续执行全部迁移；这是既有迁移链的种子数据前置条件，不是 055 新增的问题。

验证结果：

- `persona_reports.kol_id`、`kol_intake_links.kol_id`、`kol_intake_operator_sessions.kol_id` 均可空。
- 三条外键均指向 `kols.id`，删除正式达人时只把关联编号置空，不删除历史记录。
- 三个查询索引均存在。
- 每张目标表预置的一条历史未绑定记录在首次和第二次执行后均保留，`kol_id` 仍为空。
- 055 第二次执行只报告对象已存在，没有错误或重复对象。
- 临时迁移库验证后已删除；生产数据库未执行迁移。

回滚策略不删除列或历史数据。若上线后需停止功能，先回退应用读写；迁移新增列保持可空兼容。只有经过独立备份和数据审计后，才能另行设计删除外键、索引或列的破坏性回滚，本次不提供自动降级脚本。

## 四、本地真实链路证据

隔离验收库 `mcn_s25_browser_verify_20260803` 使用真实后端、真实权限中间件和本机假模型服务，未调用外部模型、未产生费用。

- 正式达人：`kols.id=25101`。
- 当前运营：`users.id=25001`；另一运营：`users.id=25002`。
- 最新就绪报告：`persona_reports.id=3`，`operator_id=25001`，`kol_id=25101`。
- 对应产出：`outputs.content_json.kol_id=25101`，`report_id=3`。
- 操作日志：生成和同步均记录 `kol_id=25101`，事实补空只记录字段名，不记录正文。
- 空事实字段补入 `background`、`experience`、`unique_story`、`extra_notes`；人工 `relationships` 原文保持不变。
- 非空 `persona`、`content_plan` 状态为待确认，未发生静默覆盖。

## 五、已知风险

1. 既有迁移链在纯空库重放时需要先具备模型种子编号；正式迁移前应确认目标库已有对应模型数据。
2. 本次只完成本地迁移演练，未验证生产数据量下的锁等待和执行时长。
3. 本地页面验收使用假模型验证业务编排；真实模型输出质量和响应时长仍属于产品验收项。

## 六、结论

后端实现与相关回归、并发规则、迁移安全和本地真实业务链路均通过开发侧门禁，状态为：**可供产品经理验收**。
