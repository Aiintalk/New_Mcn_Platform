# M2 Sprint26 后端测试报告：运营反馈缺陷修复 v1

> 日期：2026-08-06
> 分支：`feature/sprint26-operations-bug-fixes`
> 实际基线：`origin/main@95b25302b67dd436a5929feebcaf4bc30002e840`

## 结论

BUG-02 与 BUG-03 的后端缺陷已按“失败测试 → 最小修复 → 回归”闭环。结构不合法、修复两次仍不合法、空流、异常流、部分内容后异常、配置异常、保存非成功任务、错误标记和跨账号任务都不会被记为成功或允许保存。

开发相关测试通过；仓库全量测试通过。全局覆盖率脚本仍因仓库既有的两个分模块阈值未达而返回失败，不能据此声明覆盖率总门禁通过。

## 新增与强化的失败测试

- 千川脚本预审：严格字段和枚举、数组与必改项结构、纯文本与截断 JSON、最多两次受控修复、失败任务终态、输入保留所需回包、仅成功任务可保存、任务归属隔离。
- 千川脚本复盘：统一内容/完成/失败流事件；空流、上游异常、部分内容后异常、错误标记、零匹配诊断、失败任务保存拦截。
- 独立复审整改：配置/模型解析失败前不创建处理中任务；保存测试使用唯一任务编号，允许重复执行。

## 执行结果

| 检查 | 结果 |
|---|---|
| BUG-03 单文件聚焦回归 | 20/20 通过 |
| Sprint26 后端聚焦回归（整改前检查点） | 54/54 通过 |
| 后端单元与集成全量 | 1538 通过，1 跳过，14 条既有警告 |
| 整体覆盖率 | 70.7%，高于仓库整体目标 48% |
| 分模块覆盖率门禁 | 未通过：`services` 73.2% < 80%，`routers` 61.3% < 70% |

分模块覆盖率缺口横跨历史模块，不在 Sprint26 产品范围内；本次新增分支均有聚焦测试覆盖，未通过状态如实保留给产品经理判断，未用无关补测稀释范围。

## 数据与发布边界

- 测试只使用本地 `mcn_test`，测试结束会按夹具规则清理。
- 未执行生产数据库迁移，未连接或修改生产数据库。
- 未部署、未上线、未合并主分支。
