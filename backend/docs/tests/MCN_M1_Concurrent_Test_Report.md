# MCN Platform · M1 并发多用户测试报告

**测试日期：** 2026-06-09 14:15（模板创建，待实际执行）
**并发用户数：** 20（计划）
**测试环境：** http://localhost:8000

> **状态：⏳ 待执行** — 本报告为空模板，需要在运行中的系统上执行 `backend/tests/concurrent/test_isolation.py` 后填充实际数据。

---

## 一、数据隔离

> 验证：20 个并发用户同时操作，每个用户只能看到自己的数据。

| 编号 | 场景 | 结论 | 说明 |
|------|------|------|------|
| CON-ISOL-001 | 20 用户并发 GET /api/tasks | ⏳ | 各用户只返回自己的任务 |
| CON-ISOL-002 | 20 用户并发 GET /api/outputs | ⏳ | 各用户只返回自己的产出 |
| CON-ISOL-003 | 20 用户并发 GET /api/files | ⏳ | 各用户只返回自己的文件 |
| CON-ISOL-004 | operator A 的 task_id 被 operator B 请求 | ⏳ | 返回 403 或 404 |

## 二、竞态条件

> 验证：并发写操作不会产生数据冲突或死锁。

| 编号 | 场景 | 结论 | 说明 |
|------|------|------|------|
| CON-RACE-001 | 20 用户并发创建任务 | ⏳ | 全部成功，无重复 task_no |
| CON-RACE-002 | 同一用户并发创建 + 删除同一产出 | ⏳ | 状态一致，无脏数据 |
| CON-RACE-003 | admin 并发重置同一用户密码 | ⏳ | 不会出现密码不一致 |

## 三、性能基线

> 验证：20 并发下各接口的响应时间分布。

| 接口 | P50 | P95 | P99 | 错误率 | 结论 |
|------|-----|-----|-----|--------|------|
| GET /api/tasks | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ |
| GET /api/outputs | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ |
| GET /api/workspace/tools | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ |
| POST /api/auth/login | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ |
| GET /api/admin/users | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ |

---

## 执行说明

执行步骤：
1. 启动后端：`cd backend && uvicorn app.main:app --port 8000`
2. 运行并发测试：`cd backend && pytest tests/concurrent/ -v`
3. 将结果填入上表
4. 更新汇总

---

## 汇总

| 统计 | 值 |
|------|-----|
| 总计 | 12 项（计划） |
| 通过 | ⏳ 待执行 |
| 失败 | ⏳ 待执行 |
| 并发用户数 | 20 |

**测试结论：⏳ 待执行**
