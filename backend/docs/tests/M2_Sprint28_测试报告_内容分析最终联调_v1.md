# M2 Sprint28 测试报告：内容分析最终联调执行侧 v1

## 状态

内容执行侧源快照的新增定向回归、三线独立复审、内容分析专项、后端主体全量及正式覆盖率门禁均已通过；这些数字是组合前的历史证据。两侧现已进入独立最终组合工作树，当前组合状态与新增回归见 `M2_Sprint28_测试报告_任务配置与内容分析最终组合联调_v1.md`。所有报告均只使用注入式模拟、隔离测试数据库与固定时钟，不代表真实飞书、真实模型或生产联调完成。

## 自动化测试范围

- 任务信封的每日项目、周度账号和周批次收尾对象、时间窗口、批次、内部/人工重试触发来源、预检、交付目标与幂等键；根层、嵌套层和各执行分支未知字段失败关闭；内部重试与数据库原任务全字段绑定；每日/账号人工完整重试同时绑定当前任务、失败原任务和隔离域，并兼容 `manual_retry`/`retry` 真实字典别名。
- 飞书鉴权、字段读取、内容/关系完整分页、失败脱敏、范围派生和无内容语义。
- 每日项目隔离、周度共享账号单次基础分析、上下文缺失降级、四层结果摘要。
- 结构化模型输入/输出严格校验，自动测试只使用假模型。
- `outputs` 与五类内容分析持久化的事务、幂等、项目双身份和测试运行零业务投影。
- 日报项目编号/名称目录、周账号区块、收尾公司汇总、失败账号脱敏、重新收尾版本、文档键/区块键双哈希身份、防业务键或账号文本伪造标记、稳定文档更新及仅投递重试。
- 项目库分页、项目隔离、人工加入、可用状态和千川开头人工补标操作日志；停用/恢复与自动刷新并发时以最新分析快照重建跨项目机会。
- 长生命周期入口的并发会话隔离、异常资源释放、仅投递重试独立会话和构造零网络副作用。
- migration 060 在隔离临时 schema 中可重复执行。
- 同一分析键、共享目录前缀和文档键的跨数据库会话串行化；收尾零飞书输入、零模型、零项目上下文和零专业业务表重复写入。
- 已选项目但零账号实例的收尾信封、严格空批查询、全零空周报，以及非空项目范围约束。
- 收尾只允许正式隔离域，测试域收尾和人工完整收尾失败关闭；人工仅投递重试继承原正式域；终态快照版本贯穿信封、不可变结果、来源回执和内部重试核验。
- 重试模式与来源字段按运行类型闭集；失败收尾合法完整人工重试、缺少失败来源/错误模式/投递失败误走完整执行的拒绝；仅投递重试绑定原投递失败任务、内部结果和收尾版本，人工重试还必须绑定当前 TaskJob；同批次以最新自动收尾版本为权威，重试链和伪造本次任务编号均不能让旧版本覆盖。
- 会话级跨进程锁由独立数据库连接持有，覆盖业务会话中途提交；单任务嵌套锁复用同一连接，小连接池并发时预留业务连接，锁等待受任务截止时间约束；不同 `finalize_version` 的同批次收尾在完整投递前保持串行。
- 收尾内部结果编号与任务/账号/项目/窗口/域匹配、基准结构校验、可计算与不可计算账号混合、成功但无人设样本、全部不可计算和逐账号五项指标可读渲染。
- 进程内分析单飞与数据库锁等待均受截止时间约束；锁取消/解锁异常释放资源，超时保留已存内部结果语义。
- 不同结果并发写同一规范化方法键的全局唯一性，人工新增与自动/人工新增并发时的稳定身份合并，以及人工开头补标与自动刷新/补标并发时的 JSON 字段合并完整性。
- 已停用项目库记录当天再次出现时仍占用稳定身份，但不恢复可复用状态、不标记为在库可用/跨项目，也不进入跨项目机会；同项目和其他项目的可用记录共享方法键时仍保持隔离。
- 不带作品编号和外部链接的合法人工正文可进入日报历史输入，并以项目库记录编号形成仅内部使用的稳定回退身份，不阻断其他项目。

## 当前代码快照门禁证据

| 检查 | 命令 | 结果 |
|------|------|------|
| 内容分析专项及相关接口/迁移回归 | `pytest -q --no-cov tests/unit/services/content_analysis tests/unit/services/test_yunwu_adapter.py tests/unit/models/test_content_analysis_models.py tests/unit/scripts/test_content_analysis_migration.py tests/integration/services tests/integration/routers/test_content_analysis_library.py tests/integration/routers/test_outputs.py tests/integration/routers/test_outputs_direct.py tests/integration/migrations/test_content_analysis_runtime_migration.py` | 689 通过、0 失败 |
| 完整后端主体测试 | `pytest tests/unit tests/integration -q --no-cov` | 2670 通过、1 跳过、0 失败；14 条既有 TikHub 异步模拟警告 |
| 正式覆盖率门禁 | `python scripts/run_coverage.py --gate` | 2670 通过、1 跳过、0 失败；整体 81.7%，core 100.0%、models 100.0%、services 87.6%、routers 73.4%、adapters 80.7%、middlewares 100.0%，全部通过 |
| 三线独立复审 | 合同规格、质量/安全、阶段回归 | 同一实现指纹上均为 P0=0、P1=0、P2=0；无真实外部调用 |

## 上一代码快照的历史门禁证据

以下数字产生于本轮最后一组重试、状态、并发停用及测试隔离返修之前，只用于历史追溯，不代表当前代码快照门禁结果。

| 检查 | 命令 | 结果 |
|------|------|------|
| 内容分析专项及相关接口/迁移回归 | `pytest -q tests/unit/services/content_analysis tests/unit/services/test_yunwu_adapter.py tests/unit/models/test_content_analysis_models.py tests/unit/scripts/test_content_analysis_migration.py tests/integration/services tests/integration/routers/test_content_analysis_library.py tests/integration/routers/test_outputs.py tests/integration/routers/test_outputs_direct.py tests/integration/migrations/test_content_analysis_runtime_migration.py --no-cov` | 660 通过、0 失败 |
| 失败顺序回归 | `pytest -q tests/integration/routers/test_outputs.py::TestListOutputsOperator::test_test_content_analysis_output_is_hidden_from_formal_output_center tests/integration/routers/test_tasks_direct.py --no-cov` | 8 通过、0 失败；只修正本切片测试数据前缀，未改任务测试夹具 |
| 完整后端主体测试 | `pytest tests/unit tests/integration -q --no-cov` | 2641 通过、1 跳过、0 失败；14 条既有 TikHub 异步模拟警告 |
| 正式覆盖率门禁 | `python scripts/run_coverage.py --gate` | 2641 通过、1 跳过、0 失败；整体 81.6%，core 100.0%、models 100.0%、services 87.5%、routers 73.4%、adapters 80.9%、middlewares 100.0%，全部通过 |
| 差异格式与语法 | `git diff --check`；`python -m compileall -q app` | 均通过；未发现新增凭据、环境文件、缓存或覆盖率产物 |

## TDD 红绿记录

- 周批次零账号与逐账号基准指标新增用例首次运行 7 个失败，最小实现后对应 9 个用例全部通过。
- 信封各层字段闭集、收尾版本与正式域限制新增用例首次运行 18 个失败，最小实现后合同组 50 个用例、合同相关扩展组 110 个用例全部通过。
- 缺少 `source_receipts.task_jobs.finalize_version` 的持久化回归首次运行 1 个失败，加入写入前一致性校验后 1 个通过。
- 主体全量首次发现本切片测试数据误用公共 `cov_%` 前缀导致 7 个后续清理错误；仅改为本切片 `ca_s28_%` 前缀后，失败顺序回归 8 个通过，主体全量 2612 个通过。
- 真实周账号持久化类型替换测试夹具简写后，收尾回归首次运行 1 个失败；改为按信封正式任务类型核验后 1 个通过。
- 合法收尾完整人工重试、重试模式闭集、失败来源与旧版本覆盖新增用例首次运行 5 个失败，最小实现后 6 个相关用例全部通过。
- 锁区内业务提交与旧收尾补投新增回归首次运行 3 个失败，改为独立连接会话锁并增加收尾补投来源/版本校验后 3 个全部通过；随后不同版本同批次提交回归通过。
- 非重试/测试/内部/人工重试来源字段闭集新增用例首次运行 6 个失败、1 个已有模式校验通过，最小修复后 7 个全部通过。
- 按任务配置真实嵌套结果摘要校验收尾补投来源的回归首次运行 1 个失败，修正内部结果编号读取后 1 个通过。
- 重试链旧版本、系统内部补投状态、新运行键复用、小连接池嵌套锁和失败账号明文新增回归首次运行 5 个失败、1 个已满足；最小修复后核心反例 8 个全部通过，并补充锁等待截止时间用例，相关执行/持久化/信封/渲染回归 146 个全部通过。
- 锁获取取消、解锁失败、截止时间跨越和 Python 3.10 进程内单飞异步超时新增反例首次运行合计 7 个失败，修复后资源释放、单飞与失败四层结果相关回归全部通过。
- 不同日报同方法键并发写入首次运行稳定出现 1 个唯一键冲突，将公司级事务锁前移到首次查询/排队写入前后，两个独立结果均成功且全局只保留 1 条方法记录。
- 人工开头补标与日报刷新并发反例首次运行 1 个失败，人工路径在读取前取同一事务锁后，人工开头与最新自动分析字段同时保留。
- 日报自动补标与运营人工补标并发反例首次运行 1 个失败，将 `manual_opening_updates` 纳入首次项目库读写前的同一事务锁后，人工标注不再被旧快照覆盖。
- 两个人工请求并发新增同一稳定作品身份首次运行 1 个失败，去重查询前取项目库事务锁后，两请求均成功、最终只有 1 条且来源保持人工。
- 已停用项目库内容当天再次出现的执行链路反例首次运行 1 个失败，结果错误标记为在库可用并产生跨项目机会；拆分稳定身份去重集合与可复用集合后，停用记录继续阻止重复新建，但不再进入复用和跨项目输出，对应停用/启用对照用例 2 个全部通过。
- 合法人工正文同时缺少作品编号和外部链接的历史加载反例首次运行 1 个失败并抛出“缺少稳定身份”；改为在两个外部身份都缺失时使用已持久化项目库记录编号作为内部回退键后，人工创建、日报历史加载及停用/启用对照 4 个用例全部通过。
- 停用内容与两个可用来源共享同一方法键的反例首次运行 1 个失败，停用条目错误标成跨项目；引擎与最终结果重标均增加“当前在库可用”前置条件后，停用/启用双对照及跨项目输出回归通过。
- 新版自动周批收尾存在时重试旧版系统完整收尾的反例首次运行 1 个失败，旧版仍通过原任务校验；把最新自动 `finalize_version` 校验加入系统收尾完整重试后，系统/人工完整重试及仅投递重试相关 6 个用例全部通过。
- 新版自动周批收尾先完成后执行旧版自动任务的反例首次运行 1 个失败，旧任务继续进入汇总；在周批次锁内、历史结果读取和投递之前增加自动收尾最新版本校验后，过期自动任务按合同错误关闭，自动/系统/人工收尾及补投相关 5 个用例全部通过。
- 无可信内部结果编号的周批次补投来源/版本失败首次被适配层改写成“内部结果失败”；调整四层映射顺序后保持 `failure_stage=delivery`、内部结果跳过和投递失败，不诱导完整重试。
- 每日项目与周度账号人工完整重试未绑定失败原任务的反例首次运行 2 个失败；增加当前重试任务、失败来源、执行对象、窗口、隔离域和结果/文档键核验后转绿，同时验证 `retry` 别名。
- 停用记录恢复与日报自动刷新并发时，跨项目机会曾按会话缓存中的旧分析快照重建；恢复路径把事务锁前移到首次读取前后，最终只激活最新方法。
- 账号文本包含公司汇总标记时，周报更新曾命中错误账号区块；区块键改为哈希稳定标记并仅匹配专用标记行后，收尾只更新公司汇总区块。
- 仅投递链路在参数解析、运行配置/资源创建或系统来源核验失败时，反例首次运行 3 个失败并错误落到内部结果/系统配置阶段；最小修复后均保持投递失败、内部结果跳过，不诱导完整重试。
- 模型运行期间人工停用既有项目库记录的反例首次运行 1 个失败，冻结日报仍沿用旧可用状态；模型结束后在项目库锁内重读可用状态并重新计算跨项目机会，回归转绿。
- `document_key` 含换行和伪造区块标记的反例首次运行 2 个失败；信封拒绝换行，同时文档键与区块键均改用哈希标记及固定行匹配后转绿。
- 每日/周账号人工仅投递重试缺少本次 `request_id` 和原失败任务绑定的反例首次运行 4 个失败；增加当前任务、原投递目标/隔离域、既有结果精确校验后转绿，周账号补投后的收尾继续读取原内部结果。
- 模型期间新增并停用同身份项目库内容、停用待自动补标条目的反例首次运行 2 个失败；锁内重读稳定身份与待补标集合后，前者不再进入新增候选，后者只跳过单条而不回滚日报。
- 测试清理使用固定记录例外的反例首次运行 1 个失败；改为记录每个测试实例创建的用户并限定本切片数据前缀后，跨实例数据互不删除。
- 人工补投截止时间与周批收尾锁超时曾在来源/版本核验前读取结果；新增 2 个反例后，人工来源先校验，周批补投先进入批次锁，失败路径不读取未验证结果。
- 测试域系统内部、人工完整和人工仅投递重试改变 `test_subdirectory` 的 3 个反例首次全部失败；补齐原测试子目录精确绑定后转绿，正式域规则保持不变。
- 待自动补标模型失败且条目同期被停用的反例首次运行 1 个失败；锁内重读后同时过滤更新与失败明细，该条不再产生限制码且日报继续成功。
- 信封解析、运行资源、系统配置或重试来源在读取飞书前失败的状态反例首次运行 7 个失败；统一改为飞书输入层 `skipped`，原失败阶段与重试入口保持不变。
- 任务配置实际支持对失败补投再次发起人工补投；内容侧新增两跳来源链反例首次失败，逐跳核验当前任务、前一失败任务、投递目标和同一不可变结果后，日报补投及周账号收尾回归均转绿。
- 后端主体全量首次在 3 个人设复盘用例复现外键失败：用例硬编码 `task_id=1`，但未创建自己的任务。单用例独立运行同样失败；仅在该测试文件增加归属当前测试用户的真实任务夹具后，3 个反例、单文件 20 个用例及“内容分析清理 → 人设复盘”组合 75 个用例均通过；未改人设复盘生产代码，也未放宽内容分析清理。

## 外部联调状态

未执行真实飞书、真实模型、生产数据库或付费调用。测试使用注入式假客户端、假模型、固定时钟和测试数据库；真实共享报告根目录、系统服务账号、两张表配置及应用权限待最终联调环境提供。
