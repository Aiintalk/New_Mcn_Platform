# 智能体任务配置 V1.6 阶段二测试报告（草稿）

> 验证日期：2026-09-05  
> 文档状态：开发侧草稿，等待 PM 独立验收与签收。  
> 结论：任务配置第二阶段源快照已按测试先行完成返修，单模块专项、主体全量、覆盖率、类型/静态检查、构建、双分辨率视觉验证与独立复核全部通过。该源快照现已进入指定的独立最终组合工作树；组合状态另见 Sprint28 最终组合联调测试报告，仍不得据此认定真实飞书、真实模型、正式调度或生产结果链路可用。

## 一、依据与范围

- 任务配置正式需求：`/Users/zhangchong/Desktop/codex_workspace/aiintalk_pm/docs/New_Mcn_Platform_Agents/智能体任务配置模块/智能体任务配置页面_完整需求.md`（V1.6）。
- 内容分析正式需求：`/Users/zhangchong/Desktop/codex_workspace/aiintalk_pm/docs/New_Mcn_Platform_Agents/M2_对标内容分析智能体_需求文档.md`（v2.2）。
- 接口、数据库、权限唯一事实源：`backend/docs/base/MCN_M2_Base_API.md`、`backend/docs/base/MCN_M2_Base_Database.md`、`docs/base/M1/MCN_M1_Base_Permission.md`。
- 验证迁移 058/059、配置模型、信封 2.0、刚结束业务日、3/30 个完整自然日窗口、12 小时截止和迟到结果保护。
- 验证每日项目实例、每周账号去重实例、周批次顺序、三层幂等、四层结果、受控测试隔离、完整重试和仅投递重试。
- 验证正式周批全部账号终态后的内部收尾、终态汇总、版本化重收尾、共享周报文档幂等、查询域隔离及零重复专业副作用。
- 验证九个管理员接口、标准响应信封、分页筛选、权限、操作日志脱敏、通用任务兼容及页面交互。

## 二、TDD 红灯证据

1. 合同与迁移首跑：合同模块不存在，收集阶段出现 1 个导入错误；迁移单跑因 059 文件不存在失败 1 项。
2. 运行链路首跑：执行服务模块不存在，收集阶段出现 1 个导入错误。
3. 路由阶段二首跑：周账号接口不存在、配置响应缺报告根目录、测试请求模型无法导入、运行列表不接受账号筛选，共 4 项失败。
4. 阶段一日期纠偏首跑：业务日期仍为触发日、超时摘要不是四层合同、默认关闭配置缺失，共 4 项失败。
5. 调度启动边界首跑：启动函数不存在，收集阶段出现 1 个导入错误。
6. 独立后端复核返修首跑：严格执行器结果 5 项失败；管理接口限定新增行为 6 项失败；无效执行器仍创建后台任务 1 项失败；空值兼容辅助函数缺失导致收集失败。
7. 第二轮后端边界首跑：手动重投递和内部重试收到缺字段回传时错误收敛为 `failed/analysis`（失败/分析阶段），2 项失败、13 项未选择。
8. 前端首轮合同测试：27 项中 11 项失败，证明周账号候选、请求幂等、报告根目录、账号实例、四层结果和分阶段重试尚未实现。
9. 前端独立复核返修：页面 20 项中 6 项失败，证明日测试候选范围、周账号搜索/翻页残留、延迟概览覆盖草稿、开发术语和未命名账号隐私仍需收口；最后一轮用户文案测试先失败 1 项，准确捕获页面残留开发语义。
10. PM 第一批 P1：周测试候选按已选范围派生时，后端定向测试 2 项失败、1 项通过，前端文案测试 1 项失败、19 项未选择；测试请求只先查后建时，串行/并发幂等定向测试 5 项失败；自动任务边建边等待执行器时，持久队列定向测试 3 项失败。
11. PM 第二批 P1：报告根目录未配置和测试/正式幂等键隔离首次联合定向运行 9 项失败、70 项通过；前端缺根目录提示测试 1 项失败、20 项未选择。失败分别证明空根目录仍调用执行器、旧空根目录任务可错误重试，以及测试内部结果键/文档键会与正式任务冲突。
12. 结果可见性缺口：新增正式与测试运行内部结果编号测试后，页面测试 1 项失败、20 项未选择，失败位置为找不到“内部结果编号”，准确证明此前只显示内部结果状态。
13. 用户批准周批次收尾后，合同首跑为 2 项失败、15 项通过，分别暴露执行对象枚举和收尾四层结果尚不支持；运行时测试在收集阶段因 `finalize_weekly_batch` 尚不存在失败；管理路由和前端页面各有 1 项失败，证明收尾仍显示为普通周账号对象且缺少批次标识。
14. 周批次收尾独立复审发现账号原始标识被重复写入 `result_summary.execution`。数据库直查测试覆盖报告根目录已配置和未配置两条路径，首跑均失败；最小脱敏后两项全部通过，原值只保留于内部任务载荷，结果摘要仅保留稳定哈希及批次/项目范围。
15. PM 空周报边界返修首跑 5 项中 2 项失败、3 项通过：空 `project_ids` 未被信封合同拒绝；完全无已选项目时仍错误调用一次收尾执行。另 3 项证明 `batch_size=0`、有已选项目零账号收尾和零账号查询域隔离已有基础行为。
16. 最终联调前 P1-1：UTC 输入的每日测试、周账号手动重试及显式截止时间三项新增断言首次为 3 项失败、19 项通过，失败值均保留 `+00:00`，准确证明标准信封未统一序列化为北京时间。
17. 最终联调共享锁 P1：内容分析持有 `content-analysis:weekly-batch:{weekly_batch_id}` 同键会话锁时，新版收尾创建未等待；收尾建账校验异常后任务配置会话仍留在事务中。定向首跑为 2 项失败、1 项通过；同版本并发幂等基线仍通过。

## 三、后端绿色证据

- 严格执行器合同：13 项通过。
- 阶段二运行链路返修：15 项通过。
- 管理路由完整文件：34 项通过。
- 调度启动边界：2 项通过。
- 合同、运行时、路由与调度专项：64 项通过。
- 周测试范围、测试请求幂等、持久队列、报告根目录门禁、测试/正式三键隔离及重试继承均已有专项绿色证据；信封合同与管理员路由在独立测试结构中 53 项通过。
- 周批次收尾合同文件 19 项通过；合同、调度与运行时 58 项通过；加入管理路由后的相关专项 97 项通过。覆盖末账号失败、全部失败、部分成功/无内容/失败、有已选项目的零账号空批、无已选项目零创建、投递失败后只投递、账号重试后重新收尾、并发重复幂等、查询域隔离、待处理为零，以及零输入读取/零模型/零专业业务表重复写入。
- 账号结果摘要最小化数据库直查 2 项通过，分别覆盖执行成功和报告根目录缺失未运行路径。
- 空周报边界最小返修后，原 5 项全部通过；合同、调度与运行时三个相关文件合计 58 项通过，覆盖无已选项目零创建、有已选项目零账号收尾、非负零批量、非空项目范围和零账号快照域隔离。
- 加入周批收尾、账号摘要最小化和空周报边界后的阶段性联合专项为 97 项通过。
- P1-1 最小修复后信封合同单元测试 22 项通过；UTC 每日测试、UTC 周账号手动重试和显式 UTC 截止时间均输出 `+08:00`，且三份最终信封全部通过内容侧当前严格解析器。受影响页面文件 22 项通过，TypeScript 类型检查与页面定向 ESLint 静态检查通过。本次未改调度、数据库或执行状态逻辑，按 PM 指令只跑相关单元与前端静态检查，未重跑此前主体全量和覆盖率门禁。
- 共享锁最小修复后，内容侧同键会话锁等待、新版收尾创建、异常回滚释放及同版本并发幂等定向 3 项通过；阶段二运行时完整文件 39 项通过。创建侧使用 `pg_advisory_xact_lock(hashtextextended(:lock_name, 0))`，并在同一事务内重读终态、确定版本、查重、建账和提交，执行器只在提交释放锁后调用。
- 共享锁加入后，任务信封、调度、运行时与管理员路由相关回归为 102 项通过；`tests/unit tests/integration` 主体全量为 2128 项通过、1 项跳过。覆盖率正式门禁再次执行同一主体集合并通过：核心 100.0%、模型 100.0%、服务 85.3%、路由 73.8%、适配器 80.3%、中间件 100.0%、整体 79.8%。
- 后端 8 个受影响 Python 模块以 `py_compile`（语法编译检查）通过；全仓差异格式检查 `git diff --check` 通过。

本轮后端专项、全量和覆盖率均使用名称带 `mcn_test_s28_` 前缀的显式隔离测试结构，未连接开发或生产数据库；全部验证结束后已删除本轮隔离结构。一次误用既有默认测试库时出现历史配置、候选数据及跨模块外键残留冲突，该次结果不作为产品失败或绿色证据，也没有据此修改分页或产品行为。

直接运行 `pytest tests/ -q`（收集后端全部测试目录）仍会在收集阶段被仓库既有 `tests/intake/conftest.py` 阻断：该文件在非顶层位置声明 `pytest_plugins`，新版 pytest 不再允许。该问题早于本功能且不在本轮范围；本轮未修改该文件。官方主体集合 `tests/unit tests/integration` 已完整通过。

## 四、前端绿色证据

- API 与页面定向合同：27 项通过。
- 页面独立复核返修及最新结果编号用例：21 项通过。
- 加入周批次收尾监控识别后，页面文件 22 项通过；收尾显示批次编号与账号数量且不冒充账号实例。
- API、页面与阶段一关联状态：43 项通过。
- P1 返修后的 API 与页面受影响集合：2 个文件、32 项通过；新增周批次收尾监控后页面文件 22 项通过。最终前端完整回归为 58 个测试文件、572 项全部通过。
- 默认并行首跑曾出现 `564 passed / 6 failed`（564 项通过、6 项失败），均为 5 秒等待超时；逐文件重跑及最终受控并发完整回归均通过，没有据此修改产品行为。
- TypeScript 类型检查 `npx tsc -b --pretty false` 通过；受影响 5 个实现/测试文件的 ESLint（代码规范检查）以零警告门槛通过；`npm run build` 生产构建通过并转换 3690 个模块，只保留仓库既有的凭证模块重复导入提示。
- 本轮空周报边界仅修改后端调度、信封校验和后端测试，没有前端代码、字段或页面合同变化，因此未重复运行前端 572 项、类型检查、静态检查、构建或视觉脚本；沿用上一轮已通过证据。

## 五、视觉与时区证据

视觉脚本使用注入式假接口和浏览器时区 `America/Los_Angeles`（美国洛杉矶），不连接后端、飞书、模型或真实数据。脚本明确断言：

- 每日任务为北京时间 00:00、最近 3 个完整自然日、下次 09/06 00:00。
- 每周任务为每周一北京时间 01:00、最近 30 个完整自然日、下次 09/07 01:00。
- 报告根目录准确回显，页面不出现两张输入表配置。
- 周测试按账号并展示关联项目；监控同时显示项目/账号实例与四层结果；投递失败只出现“仅重试投递”。
- 运行监控将 `weekly_batch_finalize` 显示为“周批次收尾”，展示批次编号与账号数量，不显示为账号实例。
- 1440×900 与 1024×768 均无页面外层横向溢出；1024 仅监控表容器内部形成 940/784 像素横向滚动；浏览器控制台错误为 0。

证据目录：`/Users/zhangchong/.codex/visualizations/2026/09/02/01a06340-79ee-7ba3-a8d8-d8453ee10696/agent-task-config-v1.6-stage2/`

1. `stage2-input-1440x900.png`
2. `stage2-report-root-1440x900.png`
3. `stage2-weekly-test-modal-1440x900.png`
4. `stage2-monitor-top-1440x900.png`
5. `stage2-redelivery-modal-1440x900.png`
6. `stage2-input-1024x768.png`
7. `stage2-monitor-top-1024x768.png`
8. `stage2-monitor-finalize-1024x768.png`
9. `stage2-monitor-table-scroll-1024x768.png`

以上 9 张视觉证据已在周批次收尾、内部结果编号与根目录提示返修后全部重拍。脚本直接断言新增文本、固定时区和布局合同；桌面截图可见收尾内部结果编号，窄屏分别保留页面顶部/页签定位、收尾首行和表格内部横向滚动三种滚动位置证据，不混称为同一首屏。

## 六、代码复核

- 前后端独立代码复核上一轮曾达到 `APPROVED`（批准），随后 PM 跨模块对照继续发现周测试范围、端到端测试幂等、持久队列、报告根目录门禁、测试/正式幂等身份隔离及结果编号可见性缺口。
- 上述缺口已补失败测试并完成最小返修；新增周批次收尾后，独立复核发现的账号原始键重复写入结果摘要和 PM 发现的空周报边界均已修复。本轮共享批次锁代码首轮复核未发现代码缺陷，只指出唯一事实源缺少锁合同；文档同步后复核结论为 `APPROVED`（批准），未发现 P0/P1/P2。仍需 PM 对完整证据验收，不能把开发复核代替 PM 签收。

## 七、未执行与已知限制

- 组合前源快照使用假内容分析执行器；当前组合代码已注册真实运行提供器合同，但未以真实配置启动或调用真实飞书、付费模型，也未验证正式报告写入。
- 测试运行验证带测试隔离标识的平台结构化结果（允许测试域 `Output`）与测试子目录合同；没有写正式业务投影或正式目录，包括正式日报、内容库、账号基准或跨项目机会。
- 迁移 059 只在隔离测试结构验证，未应用到开发、真实业务或生产数据库。
- 自动调度默认关闭；未配置部署系统账号，未执行自动生产触发。
- 任务配置与内容分析差异已在 PM 指定的独立最终联调工作树组合；当前仍使用假外部客户端和隔离测试库进行开发联合验收。
- 未暂存、提交、推送、创建 PR、合并、部署或上线。
