# M2 红人工作台旧版功能还原 — 测试报告

**日期：** 2026-07-14  
**分支：** `feature/kol-core-workflow`  
**范围：** 八个旧版模块还原；不含 TikTok 工具，不迁移历史业务数据。

## 结论

业务自动化回归通过，浏览器已验证红人工作台、素材库、复盘和千川成片预审入口。固定验收库为 `mcn_m1`：050、051 已验证生效，052 已在没有历史工作台配置行的前提下执行；OSS 私有上传、短时读取、替换和删除已真实连通；Gemini 统一凭证池和 `Gemini 2.5 Flash（完整视频）` 模型已启用并绑定到 `full_video`。测试达人（编号 2）已用选定的原片和剪辑成片完成一次真实 Gemini 完整视频调用：任务 9 成功，耗时 98,666 毫秒，调用日志成功，3,051 字报告已保存并成功导出。任务载荷为 `full_video`，两条视频均以完整视频对象传入；没有关键帧降级分支。

## 自动化证据

| 范围 | 命令 | 结果 |
|---|---|---|
| 八模块后端定向回归 | `pytest` 14 个路由/服务测试文件 | 227 通过 |
| 千川成片预审 | 文案预审、完整视频路由、Gemini 适配器测试 | 30 通过 |
| 前端全量回归 | `npx vitest run` | 35 文件、295 通过；另有 1 条既有种草页面测试失败，见已知问题 |
| 工作台页签修复 | 工作台默认页签单测 + 页面测试 | 后端 1 通过；前端 24 通过 |
| 素材库/复盘 | 文档解析、对象存储、素材库、复盘测试 | 已包含在后端定向回归；逐份解析、私有播放、替换删除与红人路径隔离均有覆盖 |

## 已验证的失败恢复

- 完整视频不支持的格式、单文件超过 500MB、本地临时文件写入异常、供应商异常、Gemini 凭证池为空：任务明确标记失败，临时文件清理，前端保留已选文件并禁用保存/导出。
- 素材视频上传失败：临时文件清理；删除与替换只处理当前红人路径下的私有对象。
- 复盘流式异常：返回错误事件，不生成伪“完成”报告。
- 千川仿写、价值观仿写、直播仿写：当前商品、红人上下文和人工复核/重试规则均有定向测试。

## 真实浏览器检查

**环境：** 本地后端 `127.0.0.1:8010`，本地前端 `127.0.0.1:5174`，管理员登录后使用红人工作台中的“测试达人”。

| 检查项 | 结果 |
|---|---|
| 登录、红人列表、进入工作台 | 通过 |
| 左侧页签与千川成片预审入口 | 通过；发现旧默认页签遗漏后已修复并复测 |
| 千川成片预审双文件卡、大小提示、禁用的开始按钮 | 通过；已用两条真实 mp4 完成服务端完整视频分析、保存和 Word 导出 |
| 素材库六类入口、添加素材表单、文档/视频选择控件 | 通过 |
| 复盘页可通过工作台路由打开，显示新建复盘入口 | 通过；真实复盘源文件仍待补齐 |
| 素材库真实接口 | `mcn_m1` 已验证 050 字段、051 配置；052 已执行且该库没有历史工作台配置行；复测通过 |
| OSS 真实连通性 | 通过：私有上传、15 分钟短时读取、替换、删除均成功；临时素材记录和两个远端对象均已清理 |
| 文字截断、遮挡、不可点击 | 1440×900 普通视口检查通过，未见文字截断、遮挡或业务按钮不可点击；自动化工具对“复盘”侧栏普通容器的物理点击未切换，但同一页面点击事件和路由均可打开复盘页，属于自动化驱动限制。整页截图对短页面的黑色补齐为截图工具伪影，页面实际容器背景和高度正常。 |

## 全量回归与构建已知问题

- 后端 `pytest -q` 在收集阶段被既有 `tests/intake/conftest.py` 的非顶层 `pytest_plugins` 声明阻断，未进入业务测试。八模块定向回归已通过。
- 前端 `npm run build` 仍有 20 个本分支之前的 TypeScript 类型/未使用变量错误（字幕、人格、千川合集、种草、素材库配置等）。本轮引入的价值观、千川仿写类型错误已修复。
- 前端全量测试本轮得到 295 条通过、1 条失败：`SeedingWriterPage`（种草内容仿写页）第 14 条既有测试在点击“生成种草脚本”后没有触发测试替身；相关测试与实现最后修改于本分支基线之前，且不属于本轮八模块还原改动。单独复现同样失败，已保留为基线测试债务，未越界修改无关模块。
- 本机没有 ffmpeg（视频生成工具），工作区也没有可用于真实验收的 mp4/mov 文件；未伪造视频样本。

## 外部前置条件

联合验收环境已完成以下配置和真实验证：

1. Gemini 统一凭证池、`Gemini 2.5 Flash（完整视频）` 模型及 `full_video` 绑定已在 `mcn_m1` 配置完成。
2. OSS 对象存储凭证已通过真实私有上传、读取、替换、删除验证，无需重复配置。
3. 已选定并执行：`交个朋友-weiyimei.mp4` 原片、`交个朋友-weiyimei2.mp4` 剪辑成片（均为 mp4，单条小于 500MB）。
4. 仍需将 5 份已获业务使用授权、文件名和内容均不同的真实复盘源文件放入 `backend/storage/acceptance/kol-core-workflow-20260714/retrospective_sources/`。

这些条件缺失时，系统会给出可操作错误，不会把完整视频预审悄悄替换成关键帧预审。
