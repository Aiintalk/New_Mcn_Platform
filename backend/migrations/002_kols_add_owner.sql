-- =====================================================================
-- Migration 002 — kols 表补充字段
--   1. 添加 owner VARCHAR(128)：负责人姓名（自由文本）
-- =====================================================================

ALTER TABLE kols ADD COLUMN IF NOT EXISTS owner VARCHAR(128);
