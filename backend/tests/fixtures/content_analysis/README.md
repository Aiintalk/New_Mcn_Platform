# 内容分析阶段一匿名冻结样本

本目录只保存确定性单元测试使用的合成数据。样本编号使用 `account-*` 与 `sample-*`，不包含真实姓名、账号、本机路径或长篇转写；不可把生产内容复制到这里。

`phase1_anonymized.json` 固定为 2 条最小匿名内容、1 条匿名项目关系和 1 份匿名项目上下文，覆盖播放量为零、四项互动值、可选视频引用与不可读媒体状态。`test_adapter.py` 会把样本转换为领域对象并至少驱动一次离线引擎；主分类仍由注入的基础分析器产出，不由标准输入预先携带。
